# Database Configuration (PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/eventup?connection_limit=5&pool_timeout=20&connect_timeout=60"

# NextAuth.js Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-here" # Generate using: openssl rand -base64 32

# Google OAuth (Required for authentication)
GOOGLE_CLIENT_ID="your-google-oauth-client-id"
GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret"

# Cloudflare R2 Storage Configuration (for image uploads)
CLOUDFLARE_R2_ACCOUNT_ID="your-cloudflare-account-id"
CLOUDFLARE_R2_ACCESS_KEY_ID="your-r2-access-key-id"
CLOUDFLARE_R2_SECRET_ACCESS_KEY="your-r2-secret-access-key"
CLOUDFLARE_R2_BUCKET_NAME="eventup-images"

# Optional: Vercel Analytics
VERCEL_ANALYTICS_ID=""

# Node Environment
NODE_ENV="development" # or "production" for production environment
